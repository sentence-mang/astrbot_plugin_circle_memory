# astrbot_plugin_circle_memory 重构与功能规划 v1.1（定稿）

> 状态：**定稿**（决策已全部拍板，见第九节）。原则：**不照抄竞品**——竞品是"临时注入"路线，我们是"会话组真合并"路线，只吸收其已验证的工程技巧，并适配到我们的架构与权限模型。

---

## 一、方向总览

一句话：**以「会话组真合并」为不变内核，完成 改名占位 → 架构规整 → 退出机制补全 → 竞品技巧整合 → 测试文档 → 发布** 六阶段演进。

不动的东西（内核，拒绝竞品化）：
- 组共享同一 `conversation_id` 的真合并历史（不是临时注入块）
- 邀请码 + 组管理员 + 权限隔离模型
- 组 ID 作为授权凭证、其他插件零适配接入

---

## 二、阶段 0：改名占位（先行）

**现状**：市场已有 Galaxy1108 的 `astrbot_plugin_shared_context`（先发布），我们同名撞车；我们已上架的旧条目 commit 为 `f43752b`（旧）。

**目标**：更名为未占用名，与竞品彻底区分。**最终定名：`astrbot_plugin_circle_memory`（用户指定，已核实无占用）**。

| 项 | 内容 |
|---|---|
| 新名（已定） | `astrbot_plugin_circle_memory` |
| 改动面 | 仓库名（GitHub 重命名）、`metadata.yaml` 的 name/repo URL、README 标题与链接、`main.py` 插件注册名、生产插件目录名、市场条目（claim 更新） |
| 注意 | 旧市场条目走更新而非重新发布；改名后重新推送，用户浏览器完成 claim 更新 |
| 验收 | 市场无同名；新名条目可查；生产加载正常 |

---

## 三、阶段 1：架构规整（行为不变的重构）

**现状**：`main.py` 748 行单文件：纯函数区（45-176）+ Star 类（178-748），命令、会话合并、配置、验证码混杂。

**目标**：薄壳入口 + 领域模块 + 存储层，每模块可单测。借鉴竞品的模块化（Tsukumi233 main/store 分离、qq_group_admin 多文件 + `_conf_schema.json`），但组织方式按我们的领域划分。

```
astrbot_plugin_circle_memory/
├── main.py            # 入口：Star 类、事件钩子（on_adapter_message / on_waiting_llm_request）、命令路由（薄壳）
├── commands.py        # 9 个命令处理器：create/code/join/leave/dissolve/id/list/remove/alias
├── groups.py          # 组领域纯函数：normalize/移交/解析目标/权限视图/组 ID 生成
├── codes.py           # 邀请码：生成/验证/防暴力（现 _verify_code 逻辑迁入）
├── shared_session.py  # 会话合并引擎：conversation_id 绑定、ensure、迁移、reset 语义
├── context_enhance.py # 新增：成员标注、非文本转述、历史预算/摘要（阶段 3 功能落点）
├── storage.py         # 配置读写：原子写（tmp+os.replace）、归档、迁移
├── constants.py       # 常量与配置 schema 表（对齐 _conf_schema.json）
├── _conf_schema.json  # 配置项 schema（新增全部配置项）
├── metadata.yaml / README.md / test_pure.py / test_integration.py
```

**关键设计原则**：
1. 事件钩子只做「取参 → 调领域函数 → 回结果」，逻辑全部下沉到模块
2. 纯函数保持纯（不依赖 event/config），延续现有 `test_pure.py` 模式
3. 配置读写只经 `storage.py`（统一原子写、BOM 兼容、迁移入口）
4. 行为冻结：重构期间功能零变化，测试全绿作为重构完成标准

**验收**：现有 23 项测试（14 纯 + 9 集成）全绿；生产加载正常；`/shared` 全命令行为与重构前一致。

---

## 四、阶段 2：退出机制补全（借鉴融合，不照抄）

**竞品对照结论**：我们 leave 核心流程扎实（原子重置→owner 移交→末人解散），缺三件竞品都有的事：**①管理员踢人 ②退出通知 ③数据别一删了之**。

### 2.1 管理员踢人 `/shared remove`（P0）
- **语法**：`/shared remove <组名> <成员>`（组内可省略组名）
- **权限**：仅组管理员（owner，即创建组会话）可操作；被移除者不能是 owner 本人（owner 移除走移交/解散流程）；平台管理员兜底
- **流程**：复用 leave 的原子退出链（重置会话 → 移出 → 若 owner 被移除则移交 → 若空组则解散/归档）
- **与竞品差异**：不引入"角色层级"，只有 owner 可踢；被踢者组 ID 立即失效（权限隔离不变）
- **文案区分**：`你已被管理员移出组「X」`（私信该成员视角） vs 组内通告

### 2.2 退出/移除通知（P0）
- leave/remove 后向组内**剩余成员**广播：`成员「昵称」已退出组「X」（剩余 N 人）`
- 主动退出 vs 被移除两种文案（借鉴 newapi 的区分设计，但发到组内而非私聊）
- 昵称来源：平台 sender_name，无则 UMO 尾段

### 2.3 归档而非物理删除（P1）
- 末人退出/解散时：共享会话历史**导出归档**（JSON 落盘到插件数据目录 `archive/`），再删会话
- 归档格式：组名、组 ID、成员、时间线消息（保留组 ID 关联，支持未来恢复）
- 保留策略：默认保留最近 N 次解散归档（默认 10，可配置）；磁盘占用警示
- 可选扩展（P2）：`/shared restore <归档编号>` 重建组

### 2.4 leave 失败净化提示（P1）
- 现 `_save_user_groups` 失败静默 → 补明确报错：`成员已移除但配置保存失败，请重试`
- 保留现有"重置会话失败即取消退出"的原子性

### 2.5 退出/被踢成员的数据处理（P0，已定稿）
**数据分层**：
- 组共享历史 = 组资产（含该成员发言），不退给个人
- 个人会话 = 该成员私有，退出即重置
- 组 ID 访问权 = 退出即失效（权限模型不变）

**策略（已定：双策略实现，配置项 `exit_data_policy`，默认 `discard`）**：
| 策略 | 行为 | 优劣 |
|---|---|---|
| `discard`（默认） | 退出/被踢 → 会话重置为空；组历史留给剩余成员；退出者无副本 | 权限最干净、零成本；被踢者丢自己的发言 |
| `mine_only` | 在 discard 基础上，导出该成员**自己的发言**（按 sender 过滤，不含他人发言/系统注入）→ 存服务器 `archive/personal/` + 尽力私聊发送 | 尊重个人数据；不泄露组隐私；被踢者可留底 |

**私聊发送失败处理（已定：反馈失败并存储）**：发送失败时明确回复 `你的发言记录已保存到服务器，但私聊发送失败（原因）`，不静默吞掉。

**规则**：
- 被踢与主动退出**数据同样处理**（区别只在文案/流程触发方），不因被踢而获得额外数据权
- 组未解散时，退出者重新 join 可恢复组历史可见（组历史从未删除，只是失去访问权；踢错人=重新邀请）
- 末人退出/组解散 → 整组历史归档服务器（2.3），不发给退出者；若解散者策略为 `mine_only`，其个人发言按策略导出

**验收**：test_pure 覆盖 remove 权限矩阵（owner/成员/组外/平台管理员）；test_integration 覆盖 踢人→被踢者 ID 不可见→通知到达；归档文件生成与保留策略生效。

---

## 五、阶段 3：竞品技巧整合（适配真合并架构）

> 每项标注竞品出处 + 我们的适配形态。**不做**的：临时注入架构、群聊感知增强、手工逐会话记忆管理（路线冲突）。

### P0 级

| # | 功能 | 竞品出处 | 我们的适配形态 |
|---|---|---|---|
| 1 | **非文本组件策略** | Galaxy1108 `file_component_mode`（4 模式）+ muyouzhi6 失效图片清理 | 配置 `media_mode`：`ignore`/`placeholder`/`caption`/`full`。**适配点**：真合并历史入库前按模式转成文本/占位；跨平台失效图片 URL 入库时即降级占位（不等请求时才发现） |
| 2 | **成员身份标注** | muyouzhi6 context_aware（2100 下载）+ Maxuix 昵称映射 | 共享历史消息前缀 `[平台:昵称]`（入库时写入，非请求时拼装）；`/shared alias <昵称>` 组内昵称映射表；LLM 不再看 UMO 乱码 |
| 3 | **历史预算与摘要** | Galaxy1108 `_adaptive_budget` + syydmtl 摘要压缩 | 真合并历史超阈值时：**最近 N 条全量 + 头部 AI 摘要 + 中段折叠**（折叠点打标记，新消息触发重摘要）；预算/折叠阈值可配置；摘要用当前会话 provider，失败自动退化为纯截断 |

### P1 级

| # | 功能 | 竞品出处 | 我们的适配形态 |
|---|---|---|---|
| 4 | 消息去重 | Tsukumi233 指纹 + TTL 180s | 入库前消息指纹（平台+ID+文本 hash）查重，防事件重复触发双写 |
| 5 | 命令消息过滤 | Galaxy1108 `skip_command` | `/shared` 系列命令及 `/` 开头命令不入共享历史（可配置开关） |
| 6 | reset 语义明确 | Tsukumi233 边界标记 | 定义并文档化：组内成员 `/reset` 只重置**个人视角**（本会话指针），不破坏组历史；组历史 reset 仅 owner 可用（新命令或文档约定） |
| 7 | 提示词单次注入 | zz6zz666 清洗策略 | 共享会话的 system 说明（如"这是共享会话组"）只放 prompt 层注入一次，不进历史层，杜绝累积 |

### P2 级（已定：全部纳入）

| # | 功能 | 说明 |
|---|---|---|
| 8 | 组置顶记忆 pin | owner `/shared pin <内容>`，置顶每轮在组上下文最前 |
| 9 | 图片轮数控制 | 只保留最后 N 轮用户消息图片，更早转占位（省 token） |
| 10 | 批量退出 | `/shared leave all` / 按 list 序号退出 |
| 11 | dissolve 二次确认 | `dissolve <组名> 确认` |
| 12 | 增量注入保缓存 | 第二次请求起只注入新记录（**适配难点**：真合并历史天然连续，需与预算折叠机制协调设计，倾向"折叠+增量游标"结合） |
| 13 | 工具式组摘要 | 组外管理员可让 bot 按需读取组摘要 |

---

## 六、阶段 4：测试与文档

- `test_pure.py` 扩展：codes 防暴力、groups 权限矩阵（含 remove）、storage 原子写与归档、context_enhance 标注/折叠/预算
- `test_integration.py` 扩展：remove 全链路、通知、归档生成、成员标注入库、命令过滤
- README 重写：新架构说明、命令表（+remove/alias）、配置表（media_mode/预算/归档）、权限模型、FAQ
- 版本号：`1.2.0`（功能增量；架构重构不单独升主版本）

---

## 七、阶段 5：发布

1. 推送改名后的仓库（新名、新版本）
2. 生产同步（新目录名 + 旧目录清理 + 配置迁移保持数据）
3. 市场更新（claim 流程）——用户浏览器操作清单同 `UPLOAD_GUIDE.md` 更新
4. 安全复核（沿用上轮 VirusTotal/静态审查流程，新增模块同样无网络/无 exec）

---

## 八、执行顺序与依赖

```
改名占位(0) → 架构重构(1) → 退出机制(2) → P0 技巧(3a) → P1 技巧(3b) → 测试文档(4) → 发布(5)
     ↑互不依赖可并行            ↑依赖 1 的模块边界            ↑依赖 3 完成
```

估算：阶段 1 约 1 次提交规模，阶段 2+3 各独立提交，每阶段测试绿了再进下一阶段。

---

## 九、决策记录（已全部拍板 ✅）

| # | 决策点 | 结果 |
|---|---|---|
| 1 | 新名字 | **`astrbot_plugin_circle_memory`**（用户指定） |
| 2 | 踢人权限 | ✅ 同意：owner 可踢任意成员；owner 本人不可被踢（需移交或解散） |
| 3 | 归档保留 | ✅ 同意：保留最近 10 份，过期自动清理 |
| 4 | 摘要依赖 LLM | ✅ 接受（用当前会话 provider） |
| 5 | P2 清单 | ✅ **全部纳入**（8-13 项） |
| 6 | 版本号 | ✅ `1.2.0` |
| 7 | 退出数据策略 | ✅ **双策略实现**（`exit_data_policy`：discard 默认 / mine_only），配置切换 |
| 8 | mine_only 降级 | ✅ **反馈失败并存储**：明确告知"已保存到服务器但私聊发送失败"，不静默 |
